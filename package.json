{
  "name": "platonic-dice-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "jest": "^29.7.0"
  }
}
