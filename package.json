{
  "name": "platonic-dice-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.0.9",
    "@vitest/coverage-v8": "^4.0.9",
    "vite-tsconfig-paths": "^5.1.4",
    "jest": "^29.7.0",
    "tsd": "^0.31.2",
    "ts-jest": "^29.4.5",
    "@types/jest": "^30.0.0"
  }
}
