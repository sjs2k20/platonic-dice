/**
 * @module @dice/core/src/validators/testConditions
 * @description
 * Validation helpers for {@link TestConditions}.
 *
 * Provides logic for normalising, checking, and validating
 * `TestConditions` used in roll testing systems.
 *
 * Used internally by roll testing and entity validation layers.
 *
 * @private
 *
 * @example
 * import { validateTestCondition } from "#validators/testConditions";
 */
export { isValidTestCondition } from "./validators.js";
